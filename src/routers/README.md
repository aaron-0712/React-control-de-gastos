# Routers

Aquí se encuentra el "mapa" de navegación de la aplicación usando **React Router**.

- **routes.tsx:** Definimos qué componente se muestra según la URL (ej: `/` -> Home, `/login` -> Login).
- Aquí se gestionan también las **Rutas Protegidas** (solo para usuarios logueados).
